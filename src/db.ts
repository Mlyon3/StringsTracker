import Dexie,{type EntityTable} from 'dexie'; import type {Asset,MaintenanceEvent,Reminder,StringInstallation,UsualSetup} from './types';
export class JournalDB extends Dexie { assets!:EntityTable<Asset,'id'>; events!:EntityTable<MaintenanceEvent,'id'>; installations!:EntityTable<StringInstallation,'id'>; reminders!:EntityTable<Reminder,'id'>; usualSetups!:EntityTable<UsualSetup,'assetId'>; constructor(name='string-ledger'){super(name);this.version(1).stores({assets:'id,status,profileType,createdAt',events:'id,assetId,date,eventType',installations:'id,assetId,maintenanceEventId,[assetId+position],removedDate',reminders:'id,assetId,dueDate,status',usualSetups:'assetId'});this.version(2).stores({reminders:'id,assetId,maintenanceEventId,dueDate,status'});} }
export const db=new JournalDB(); export const uid=()=>crypto.randomUUID(); export const today=()=>new Date().toISOString().slice(0,10);
